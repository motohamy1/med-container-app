# Taste

## UI / Design

- Prefers tight, precise spacing between scrollable content and floating UI elements (e.g. tab bars); will often specify exact pixel values (e.g. "10px or less") rather than accept generous default padding. Confidence: 0.8
